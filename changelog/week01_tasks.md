- code 2 sum solution. It would be a single function in a python file with a parameter for input and algorithm to use.  Have four algorithms implemented - brute force, optimized, edge cases (what if array is sorted? What if array is extremely large? ) solution is guaranteed to exist. 

- 1 hour create an empty git repository on your pc. Create a branch from it. Commit code done above. Push it to GitHub profile. Setup ssh token for GitHub. Create a pull request and format it. 

- work on creating a to do list in python, push it to a different branch. Create a pull request
